# decentralize this configuration
enter ~\/.*\/(.*)
  script="$PWD/.ondir/enter"
  [[ -r "$script" ]] && . "$script"

leave ~\/.*\/(.*)
  script="$OLDPWD/.ondir/leave"
  [[ -r "$script" ]] && . "$script"
